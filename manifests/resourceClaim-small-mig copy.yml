apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  namespace: ollama-test
  name: custom-request
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: mig.nvidia.com
            count: 1
            selectors:
              - cel:
                  expression: device.attributes["gpu.nvidia.com"].memory > 10000Mi
